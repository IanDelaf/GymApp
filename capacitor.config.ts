import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Just Another App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
